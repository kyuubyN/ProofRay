# Security policy

This private alpha has no supported public release. Do not place secrets in an
issue. Report suspected vulnerabilities privately to the repository owner via
GitHub's private vulnerability reporting once enabled.

The remote-model adapters are outside the offline core. Network access must be
explicitly enabled by the application, and secrets must remain outside prompts,
URLs, ledgers and exception text.
